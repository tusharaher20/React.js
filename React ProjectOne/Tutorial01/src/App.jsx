import Card from "./Card";
import Button from "./Button/Button";
import Student from "./Students";
function App() {
  return (
    <>
      {/* <Student name="SpongeBob" age="Tushar" isStudent={false} />
      <Student name="Youtuber Tushar" age={23} isStudent={true} />
      <Student name="Aher Tushar" age={33} isStudent={false} />
      <Student name="Greatest of all Time" age={33} isStudent={true} /> */}
      {/* everything I have added above is a Key,  */}

      {/* <Student name="Lary" /> */}
      {/* the default props example is just above oh yeah! */}

      {/* Started with Conditional Rendering and it's fun */}

    </>
  );
}

/* Props:JavaScript, "props" (short for "properties") refers to the values or arguments
 passed into a function or component. A parent component can send data to a child component */

/*  In React, defaultProps is an optional property that you can define on a React component. 
It allows you to specify default values for certain props in case they are not provided by the 
parent component when the child component is rendered.

*/

export default App;
