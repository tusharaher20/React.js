function Mummy() {
  /*the return statement is used inside a component function to specify what should be rendered on the screen. 
 A React component is essentially a JavaScript function that returns React elements (which describe what should be displayed). 
 These elements can be simple text, HTML tags, or other React components.*/

  return (
    <header>
      <h1>My Website is ready to be published and I am happy for it</h1>
    </header>
  );
}

export default Mummy;
